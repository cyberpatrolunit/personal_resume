import styles from "./icon.module.scss";

const Icon = () => {
    return (
        <div className={styles.iconWrapper} aria-hidden="true">
            <span />
            <span />
            <span />
        </div>
    );
};

export default Icon;
